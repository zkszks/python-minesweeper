[project]
name = "minesweeper"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "fzycc", email = "chaijunning92829@hotmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "pygame>=2.6.1",
]

[project.scripts]
minesweeper = "minesweeper:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
